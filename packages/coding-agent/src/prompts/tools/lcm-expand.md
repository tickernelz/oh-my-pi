Expand one opaque LCM summary handle within the current child/task session and branch. Returns only depth-, item-, and token-bounded redacted summary children or authoritative journal-derived source slices. File references remain metadata-only, unavailable sources are reported explicitly, and scope never crosses the active project/session/branch. This tool is never available to the top-level agent.